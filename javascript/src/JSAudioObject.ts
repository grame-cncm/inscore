class AudioObject {
    fAudioNode : AudioNode;

    connect(toConnect: AudioObject) {
        // fAudioNode connect /w toConnect
    }

    disconnect(toDisconnect: AudioObject) {
        // fAudioNode disconnect /w toDisconnect
    }

    update() {
        // TODO: Get information on the prupose of this function
    }
}