
///<reference path="classA.ts"/>


class ACaller {
    protected fMethod: AMethod;    
    constructor(method: AMethod) { this.fMethod = method; }    

    handle(n: number) { 
		console.log("handling " + n);
    	this.fMethod( n ); 
    }
}
class ACaller2 {
    protected fMethod: AMethod2;    
    constructor(method: AMethod2) { this.fMethod = method; }    

    handle(n: number, m: number) { 
    	this.fMethod( n, m ); 
    }
}
