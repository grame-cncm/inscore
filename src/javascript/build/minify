#!/bin/sh
#

echo "curl -X POST -s --data-urlencode 'input@$1.js' http://javascript-minifier.com/raw > $1.min.js"
curl -X POST -s --data-urlencode 'input@$1.js' http://javascript-minifier.com/raw > $1.min.js
exit $?